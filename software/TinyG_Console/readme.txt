To run this TinyG Console (hence TG-Console.py) you need pyserial on your system.
Just run:

python TG-Console.py 

from the command line and everything else should just work.  The menus are pretty intuitive.

Ril3y
